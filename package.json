{
  "name": "growth-os",
  "version": "1.0.0",
  "private": true,
  "description": "Growth OS — Motor de prospecção ativa, extração de leads e funil de conversão autônomo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
